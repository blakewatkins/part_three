spring:
  profiles: dev
  cloud:
    stream:
      bindings:
        eventConsumer-in-0:
          destination: mdms.dev.test.test,mdms.dev.test.test1
          binder: kafka1
logging:
  level:
    com.amway.dms.mdmseventkeystoglobalprocessor: TRACE